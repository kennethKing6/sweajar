import React, { useEffect, useState } from "react";
import { AppState } from "../model/AppState";
import NavBar from "./NavBar";

export default function Tutorial({
    onPress = () => {},
}) {
    return (
        <div>
            <p>TUTORIAL GOES HERE</p>
        </div>
    );
}
